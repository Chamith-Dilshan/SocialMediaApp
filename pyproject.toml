[project]
name = "socialmediaapp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "uvicorn>=0.50.0",
    "pydantic>=2.13.4",
    "httpx2>=2.5.0",
    "pytest>=9.1.1",
]

[tool.fastapi]
entrypoint = "app.main:app"